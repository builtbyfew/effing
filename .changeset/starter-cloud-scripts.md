---
"@effing/create": minor
---

Add effing-cloud scripts to starter template

Adds `cloud:deploy` and `cloud:url-secret` scripts wired to the `effing-cloud`
CLI, along with an `effing-cloud.config.ts`.
