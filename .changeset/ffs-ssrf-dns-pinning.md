---
"@effing/ffs": patch
---

Pin outbound fetches to the DNS results that passed SSRF validation (closing a DNS-rebinding window), use a timing-safe API key comparison, and document the security-related environment variables.
