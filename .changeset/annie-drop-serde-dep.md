---
"@effing/annie": patch
---

Drop unused `@effing/serde` dependency

`@effing/annie` no longer imports anything from `@effing/serde`, so the
runtime dependency has been removed. No public API change.
