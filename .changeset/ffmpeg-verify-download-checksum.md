---
"@effing/ffmpeg": patch
---

Verify the SHA-256 checksum of the downloaded FFmpeg binary against pinned per-platform digests; set `FFMPEG_SKIP_CHECKSUM=1` to opt out for custom builds served via `FFMPEG_BINARIES_URL`.
