---
"@effing/satori": minor
---

Add `satoriPoolPlugin()` Vite plugin (`@effing/satori/vite`) that bundles the satori worker into the SSR output and rewrites `createSatoriPool()` calls to point at it
