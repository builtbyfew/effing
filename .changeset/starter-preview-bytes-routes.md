---
"@effing/create": minor
---

Add agent-friendly preview routes for images and annies (`/preview/image/:imageId.bytes`, `/preview/annie/:annieId.tar`) that stream the preview-rendered output directly, mirroring the existing `/preview/effie/:effieId.json` endpoint
